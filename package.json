{
  "name": "quicklib",
  "version": "1.0.0",
  "description": "Minimal read-only Calibre library web browser",
  "main": "src/app.js",
  "scripts": {
    "start": "node --env-file=.env src/app.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "express-session": "^1.18.1"
  },
  "engines": {
    "node": ">=24"
  }
}
